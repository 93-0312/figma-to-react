# Figma 변경 감지(Phase 1)용 Personal Access Token.
# 발급: figma.com → Settings → Security → Personal access tokens (scope: File content = read)
# 실제 값은 .env 에 넣고 절대 커밋하지 말 것.
# 형식: figd_ 로 시작하는 토큰. 실제 값은 .env 에만 넣을 것(이 파일엔 금지).
FIGMA_TOKEN=PUT_YOUR_FIGMA_PERSONAL_ACCESS_TOKEN_HERE
